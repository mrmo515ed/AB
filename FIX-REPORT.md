# تقرير التحديث الشامل — Anime Black (أنمي بلاك)

**التاريخ:** 2026-09-23 — التحديث الثالث «التحديث الأسطوري الشامل» (يتضمن أيضاً إصلاح المزامنة اللحظية من التحديث الثاني)
**النطاق:** إزالة كاملة للإيموجي + نظام قوائم فائضة موحّد + دردشات متخصصة للقروبات/العوالم/النقابات + إكمال PWA + تحسينات السرعة + إصلاح المزامنة اللحظية + تأمين الأساس.

---

## ما الجديد في التحديث الأسطوري (v7.3)

### 1) صفر إيموجي — أيقونات احترافية في كل مكان
- أُزيلت **639 مجموعة إيموجي** من كامل التطبيق (قوائم، أزرار، توستات، رسائل نظام) واستُبدلت بنظام الأيقونات SVG الاحترافي المدمج (127+ أيقونة).
- أُصلحت الصور الرمزية المعطوبة التي كانت تستخدم إيموجي كسلسلة نصية.
- تحقق آلي: اختبار `task3_features` يمسح المستند بالكامل — **صفر إيموجي** (مؤكد مستمرًا في CI).

### 2) نظام القوائم الفائضة الموحّد (Overflow Menus)
بنية جديدة `openMoreMenu` / `moreBtnV` قابلة لإعادة الاستخدام في أي صفحة — زر «المزيد» يفتح لوحة خيارات بأيقونة وعنوان ووصف لكل إجراء:
- **رأس المحادثة الخاصة:** انخفض من 6 أزرار إلى 3 (رجوع/بحث/المزيد) — المكالمات الصوتية والمرئية وخيارات المحادثة والملف الشخصية انتقلت لقائمة فائضة أنيقة.
- **شريط تحديد الرسائل:** التوجيه والحذف والخروج ظاهرة، والتمييز ونسخ النصوص وتحديد الكل في القائمة الفائضة.
- البنية جاهزة لأي منطقة تزدحم مستقبلاً بتغيير سطر واحد.

### 3) دردشات متخصصة لكل نوع (ليست نسخاً مكررة)
**القروبات (مجموعات):**
- **تفاعلات رسائل** (8 أنواع بأيقونات وجه احترافية: محبة/ضحك/دهشة/غضب/حزن/حماس/احتفال/تحدي) تعرض عدّاداً ويمكن التبديل بنقرة.
- **تعديل رسائلك** مع شارة «عُدّلت» + حذف + تثبيت + ردود + إعلانات + تسجيل صوتي (كانت موجودة وأصبحت مكتملة).
- **إحصائيات القروب**: رسائل/أعضاء/نشاط 24 ساعة/وسائط/صوتيات/استفتاءات + أكثر 5 مشاركين.
- **معرض الوسائط** بشبكة صور وفيديو مع عارض ملء الشاشة.
- **بحث فوري** في كل رسائل القروب مع نتائج غنية.
- **وضع التهدئة** للمشرفين (30 ثانية بين رسائل الأعضاء).

**العوالم:**
- تفاعلات + تعديل + أوامر سريعة (كالقروبات).
- **فعاليات العالم**: جدولة فعاليات بتاريخ ووقت ووصف وحذف — لمالك العالم.
- **لوحة النشاط الآن**: آخر المتحدثين مع آخر ظهور وزر مراسلة مباشر.

**النقابات والمجتمعات:**
- **نظام رتب ومساهمات تلقائي**: نقاط لكل رسالة في قنوات النقابة ولكل تبرع للخزينة — رتب مبتدئ/محارب/نخبة/أسطورة بشريط تقدم وسلم رتب وأعلى 10 مساهمين.
- إشعارات ترقية فورية عند الوصول لرتبة جديدة.
- (بالإضافة للقنوات والرتب الإدارية والخزينة وسجل التبرعات الموجودة أصلاً).

**أوامر سريعة (Slash) للقروبات والعوالم:**
- `/me فعل` — رسالة حدث، `/roll 2d6` — نرد، `/poll سؤال | خيار | خيار` — استفتاء فوري، `/topic نص` — تحديث الموضوع (إدارة)، `/clear` — تنظيف الرسائل (إدارة).

### 4) PWA مكتمل + سرعة فائقة
- **اختصارات تطبيق** (App Shortcuts): التغذية/المحادثات/العوالم/المجتمعات من سطح المكتب — مع موجّه `?go=` يفتح الصفحة المطلوبة مباشرة.
- **مشاركة إلى التطبيق** (Share Target): مشاركة نصوص وروابط من أي تطبيق إلى أنمي بلاك.
- `display_override` (standalone ثم minimal-ui) + `id` ثابت.
- طبقات كاش الخدمة (SW) محدثة v7.3: شبكة-أولاً للمستندات، Stale-While-Revalidate للأصول — تصفح فوري مع عمل دون اتصال.
- **أداء العرض**: `content-visibility` للمنشورات (تخطي تصيير ما خارج الشاشة)، `touch-action: manipulation` (لا تأخير نقر مزدوج)، `overscroll-behavior` لمنع السحب المطاطي، واحترام `prefers-reduced-motion`.

### 5) المزامنة اللحظية الفورية
مدير المزامنة المركزي `rtm.js` من التحديث السابق يعمل بكامل طاقته: إعادة اتصال تلقائية بتراجع أُسّي، مستمع كناري حقيقي، إيقاف مستمعات الحساب عند الخروج/التبديل — تفاصيلها كاملة في القسم التالي.

---

---

## أولاً: السبب الجذري لـ"توقف المزامنة بعد فترة" وما أُصلح

### 1) لم يكن هناك أي مدير لمستمعات Firestore (~18 مستمعاً مبعثراً)
أي خطأ دائم في مستمع (permission-denied / failed-precondition / فشل قناة) كان **يقتله للأبد بصمت** — التطبيق يبدو عاملاً من الكاش المحلي ثم "تتوقف المزامنة".

**الحل: مدير المزامنة المركزي `rtm.js`** (ملف مستقل، يُحمَّل قبل الجسر، يُختبر مباشرة في vitest):
- كل مستمع في التطبيق كله يمر عبر `window.onSnapshot` المُعاد تعريفه → المدير (تحقق آلي: **صفر** استدعاءات onSnapshot خام خارج طبقة المدير).
- كل مستمع يحمل: **معرف فريد، حالة (نشط/إعادة اتصال/فاشل/متوقف)، آخر لقطة ناجحة، آخر خطأ برمزه، عدد المحاولات، دالة إلغاء**.
- خطأ مؤقت → **إعادة اشتراك تلقائية بتراجع أُسّي + jitter** (1ث→60ث، يتصفّر عند النجاح).
- خطأ دائم → حالة `failed` + **إعادة محاولة بطيئة كل 5 دقائق للأبد** + console.error + توست تحذير + ظهور في شاشة التشخيص — **لا فقدان صامت إطلاقاً**.
- منع التكرار: نفس المسار يستبدل القديم (أصلح تسريب مستمع الإعدادات البصرية ومساحة العمل).
- سقف صارم 64 مستمعاً مع استبدال الأقدم.
- إعادة بناء تلقائية عند: **استعادة الشبكة، عودة التبويب للظهور، استئناف التطبيق/PWA (pageshow)، التركيز، تغيّر حالة المصادقة، عودة الاتصال (مستمع كناري حقيقي ببيانات وصفية)** + نبضة مشرف كل 30 ثانية تنقذ المؤقتات المخنوقة في الخلفية.
- إيقاف مستمعات الحساب (الدردشات/الإشعارات/ملفي/الغرف/مساحة العمل) تلقائياً عند **الخروج وتبديل الحساب** — كان تبديل الحساب يترك مستمعات الحساب القديم حية.

### 2) `syncUserStateToServer` كان يبلع الفشل ويكتب المحلي فوق السيرفر
- استُبدلت بوعود حقيقية: كل عملية كتابة تعيد `{op, path, success, code, retryable}` — لا `Promise.allSettled` تبلع الإخفاقات.
- **بوابة تسوية**: قراءة طابع السيرفر عبر `getDocFromServer` قبل الدفع — إن كانت حالة السيرفر أحدث تُسحب بدل الكتابة فوقها (السيرفر مصدر الحقيقة: SERVER→CACHE→UI).
- كل توستات "النجاح الوهمي" أُزيلت: زر المزامنة، نشر/حذف الملاحظة، البث الإداري (موقعان) — النجاح يُعلن **فقط بعد تأكيد السيرفر الفعلي**.
- شارة "متصل ومزامن لحظياً" **الملفقة** في الإعدادات صارت قيمة حقيقية من المدير.

### 3) حزما SDK متعارضتان تعملان معاً
- كانت الصفحة تشغّل CDN v10.10.0 **و** نسخة v12 كاملة داخل dist تُنشئ تطبيق Firebase ثانياً (مثيلَي مصادقة + كاشان مستقلان + سباقات رموز).
- **توحيد كامل**: إصدار واحد 10.10.0 في كل مكان؛ طبقة src أصبحت lazy وتعيد استخدام مثيل الجسر نفسه (لا تطبيق ثانٍ أبداً). الحزمة نحفت من 1026KB إلى **756KB**.

### 4) أخطاء أخرى مصلحة
- `setLogLevel('silent')` كان يكتم كل أخطاء Firebase → الآن `error` + وضع تشخيص موسّع (`localStorage animeblack_fb_debug=1`).
- خطة إنقاذ قاعدة البيانات المسماة: إن ثبت عدم وجودها على السيرفر → تحويل تلقائي لمرة واحدة للافتراضية + إعادة تحميل (بلا حلقات).
- مستمعات بلا حدود (communities/groups/worlds/broadcasts/level_badges/غرف/مساحة العمل) → حدود صريحة.
- خطأ e2e: فشل الشبكة الخارجية كان يرجع 500 → الآن 502 بالمعنى الصحيح.

---

## ثانياً: تشديد قواعد الأمان (بلا أي إضعاف)

| الثغرة | الإصلاح |
|---|---|
| **أي مستخدم يستطيع تعيين نفسه Admin** (إنشاء/تحديث ملفه بدور متميز) | سُدت تماماً: الأدوار المتميزة محجوبة في الإنشاء والتحديث الذاتي |
| **أي مستخدم موقّع يقرأ أي محادثة خاصة** (chats وmessages: `isSignedIn` فقط) | القراءة/التحديث للمشاركين الفعليين فقط (مع تسامح مع محادثات قديمة بلا participants) |
| إيصالات القراءة `st` والإعجابات `likedUsers/lastReaction` وتصويتات `poll` **كانت القواعد ترفضها** (مزامنة مكسورة فعلياً!) | أُضيفت للمفاتيح المسموحة |
| انتحال الإشعارات (أي أحد ينشئ إشعاراً لأي أحد) | الإنشاء بصفتك الفاعل الفعلي (fromUserId/senderId) أو أدمن |
| سجلات التدقيق قابلة للكتابة من أي موقّع | أدمن فقط |
| لوحات صدارة الألعاب والمباريات قابلة للكتابة من أي موقّع (التطبيق لا يكتبها أصلاً) | كتابة إدارية فقط |
| تحكم كامل بأي قروب/مجتمع/عالم من أي موقّع | مفاتيح محدودة لغير المشرف/المالك (انضمام/مغادرة/آخر رسالة) + حماية حقول الملكية والإدارة |
| رسائل العوالم مقروءة للعامة (حتى غير الموقعين) | للموقعين فقط |

---

## ثالثاً: الأدلة (فحوصات فعلية، لا ادّعاءات)

| الفحص | النتيجة |
|---|---|
| `npm install` |  lockfile مطابق 100% لـpackage.json (firebase 10.10.0) |
| `npm run check-types` |  نظيف |
| `npm run lint` |  نظيف (يشمل rtm.js الجديد) |
| `npm test` |  **49/49 عبر 8 ملفات** (منها 12 اختباراً جديداً للتحديث الأسطوري: أوامر slash، رتب النقابات والترقيات، تبديل التفاعلات، وجودة البنية الفائضة + صفر إيموجي — إضافة إلى 17 اختباراً جديداً للمدير: A→B رسائل، A→B ملف شخصي، تراجع أُسّي+jitter، خطأ دائم→failed+إعادة بطيئة، استعادة شبكة، استئناف تطبيق، نبضة قلب، منع تكرار، إيقاف scoped، تتبع كتابات برموز الأخطاء، بوابة اتجاه المزامنة، سيناريو دورة حياة كامل) |
| `npm run test:e2e` |  4/4 |
| `npm run build` |  (756KB ES + 602KB UMD + نسخ كل أصول التشغيل إلى dist/) |
| تشغيل السيرفر + فحص كل المسارات (health, /, /rtm.js, dist×2, sw, manifest) |  HTTP 200 للجميع |
| تحقق ثابت أن كل مستمع يمر بالمدير |  صفر استدعاءات خام |
| التبعيات غير المستخدمة |  أزيلت (workbox-window, puppeteer, acorn, serve, @babel/parser, @types/dompurify) + أصلح تعليق "Workbox" المضلل في sw.js |

### ما تعذّر اختباره في بيئة الفحص (بصدق كامل):
1. **اختبار متصفح حقيقي**: تنزيل Chromium محجوب في بيئة الفحص (الشبكة الخارجية مقطوعة). حلّلت دلالات المدير بمحاكاة Firestore كاملة في vitest بدلاً منه.
2. **A→B عبر Firebase حقيقي بجهازين**: يحتاج شبكة حقيقية + مشروع Firebase المنشور. **خطوات التحقق على جهازك**: افتح التطبيق → الإعدادات → "شاشة تشخيص المزامنة المباشر" — سترى المستمعات الحية وحالاتها وأخطاءها الفعلية لحظة بلحظة. جرّب: قطع الإنترنت ثم إعادته (يجب أن تعود كلها active تلقائياً)، إخفاء التبويب وإعادته، تسجيل الخروج والدخول، جهازان بحسابين لرسالة مباشرة.
3. **وجود قاعدة البيانات المسماة** `ai-studio-…`: لا يمكن التحقق عن بُعد — خطة الإنقاذ الجديدة تتعامل مع غيابها تلقائياً (لو كانت السبب الجذري عندك فسيُصلح نفسه عند أول تشغيل).

### حدود معمارية موثّقة (ليست أعطالاً):
- `members` في communities/worlds **عداد رقمي** (ليس قائمة معرّفات) — تقييد عضوية كامل يتطلب ترحيل نموذج البيانات.
- الاقتصاد (عملات/نقاط) client-authoritative بطبيعته (لا توجد Cloud Functions في المستودع) — حُمي من تصعيد الأدوار، ويوصى بمفاتيج API مستقبلية.
- بث إشعار لكل المستخدمين يحتاج Cloud Functions (الإشعار الإداري السحابي بلا userId لا يصل عبر مستمع userId) — الزر الآن يعرض النتيجة الحقيقية بصدق.

---

## الملفات المتغيرة (التحديث الأسطوري v7.3)
`index.html` (صفر إيموجي + نظام openMoreMenu/moreBtnV + تفاعلات وتعديل رسائل الغرف + إحصائيات/معرض/بحث القروب + فعاليات ونشاط العالم + رتب ومساهمات النقابات + أوامر slash + وضع التهدئة + CSS الأداء + موجّه اختصارات PWA) · `manifest.json` (اختصارات + مشاركة إليه + display_override) · `sw.js` (نسخة كاش v7.3) · `tests/task3_features.test.ts` (جديد: 12 اختباراً).

## الملفات المتغيرة (إصلاح المزامنة v7.2)
`rtm.js` + `rtm.d.ts` (المدير المركزي) · `index.html` (الجسر + syncUserStateToServer + المشرف + الكناري + شاشة syncDiag + حدود المستمعات + توستات صادقة + إفلات كامل عند الخروج) · `firestore.rules` (تشديد شامل) · `src/config/firebase.ts` + المستوردون (SDK موحد lazy) · `package.json`/`package-lock.json` (تبعيات نظيفة + firebase 10.10.0) · `sw.js` (precache rtm.js + تصحيح Workbox) · `server.js` (502 صحيح) · `tests/realtime_sync.test.ts` (جديد) · تحديث اختبارات media/auth · `eslint.config.js` · `dist/` (أعيد بناؤه).

**قاعدة صارمة محفوظة: لا حذف ولا تعطيل لأي ميزة عاملة — كل إصلاح إضافي أو تصحيحي فقط.**
